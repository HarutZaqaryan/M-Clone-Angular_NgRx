import { FirstLetterUpPipe } from './first-letter-up.pipe';

describe('FirstLetterUpPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstLetterUpPipe();
    expect(pipe).toBeTruthy();
  });
});
